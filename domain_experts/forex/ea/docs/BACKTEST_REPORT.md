# 回测报告（执行记录）

## 报告元数据

| 项目 | 值 |
|------|-----|
| 报告日期 | 2026-03-10 |
| EA 版本 | ForexStrategyExecutor V1.0.0 |
| 交易品种 | EURUSD |
| 时间周期 | H4 |
| 报告状态 | 待补充实测数据 |

## 本次已完成验证

1. 回测参数注入机制修正：当 `BacktestParamJSON` 非空时，EA 不再被文件热更新覆盖。
2. 回测 Safe Mode 恢复路径修正：内嵌参数模式下只从内嵌参数恢复，不切回文件参数源。
3. 回测日期参数生效：`BacktestStartDate` / `BacktestEndDate` 已用于限制开仓信号评估窗口。
4. 文档一致性修正：README 与运行手册的默认参数路径、回测参数行为已与代码保持一致。

## 仍需在 MT4 Strategy Tester 完成的验收项

| 任务 | 状态 | 说明 |
|------|------|------|
| 14.4 两种注入方式运行验证 | 待执行 | 需分别执行“文件参数模式”和“内嵌参数模式”回测，并留存日志 |
| 14.5 回测与实盘逻辑一致性对比 | 待执行 | 需使用同一参数包对比关键决策路径日志 |
| 14.6 回测执行与结果统计 | 待执行 | 需导出 Strategy Tester 报告并填充收益/回撤/交易统计 |

## 执行建议（MT4）

1. 在 Strategy Tester 中执行两轮回测：
   - 模式 A：`BacktestParamJSON` 为空，使用文件参数。
   - 模式 B：`BacktestParamJSON` 为单行 JSON，验证不会发生文件覆盖。
2. 导出每轮的 Experts 日志与回测结果报表（HTML/截图均可）。
3. 将实测数据填充到 `BACKTEST_REPORT_TEMPLATE.md`，并附到本报告末尾。
